<div class="container-sm">
<div style="text-align: center; padding-top: 40px"><h3>Complete Booking</h3></div>
<div class="d-flex justify-content-center">
<table class="table">
  <thead class="table-primary">
    <tr>
      <th>Starting</th>
      <th>Destination</th>
      <th>Date</th>
      <th>Departure</th>
      <th>Duration</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @travel.starting_station.city %></td>
      <td><%= @travel.destination_station.city %></td>
      <td><%= @travel.formatted_date %></td>
      <td><%= @travel.formatted_time %></td>
      <td><%= @travel.formatted_duration %></td>
    </tr>
  </tbody>
</table>
</div>
<div class="d-flex align-items-center flex-column">
<h4>Users Details...</h4>

  <%= form_with(model: @booking, local: true) do |form| %>
    <%= form.hidden_field :travel_id, value: @travel.id %>
    <%= form.hidden_field :user_id, value: current_user.id %>
    <div id='confirmation-buttons'>
      <%= link_to 'Cancel', root_path, class:"btn btn-secondary",id: 'cancel-booking' %>
      <ion-icon name="airplane-sharp", id='booking-airplane'></ion-icon>
      <%= form.submit 'Book Travel', class:"btn btn-primary", id: 'book-travel' %>
    </div>
  <% end %>
  </div>
  </div>